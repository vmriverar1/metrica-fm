'use client';

import React from 'react';
import UnifiedDashboard from '@/components/admin/dynamic-content/UnifiedDashboard';

export default function DynamicContentPage() {
  return <UnifiedDashboard />;
}